# 本地化工具完整流程测试计划

## 1. 测试目标
执行本地化工具中的每一个流程，确保完整运行并验证最终结果的正确性，包括：
- Extract模式(英文提取)
- Extract模式(中文提取)
- Extend模式(英文映射到中文)
- Extend模式(中文映射到英文)

## 2. 测试环境
- **测试目录**：D:\Poki\Tool\Localization_File
- **工具目录**：D:\Poki\Tool\Localization_Tool
- **测试文件夹结构**：按照用户要求使用实际目录结构

## 3. 测试步骤

### 3.1 准备工作
1. **检查目录结构**：查看Localization_File目录的当前结构
2. **创建测试文件**：确保测试所需的文件和目录存在
3. **备份原始文件**：对测试目录进行备份，以便恢复

### 3.2 Extract模式测试
#### 3.2.1 英文提取流程
1. **执行命令**：`python src/main.py extract "英文提取流程"`
2. **验证结果**：
   - 检查是否生成 `output/Extract_English/` 目录
   - 验证 `English_mappings.json` 和 `English_mappings.yaml` 文件生成
   - 验证 `extract_{timestamp}_report.json` 文件生成
   - 验证 `mod_info.json` 文件生成

#### 3.2.2 中文提取流程
1. **执行命令**：`python src/main.py extract "中文提取流程"`
2. **验证结果**：
   - 检查是否生成 `output/Extract_Chinese/` 目录
   - 验证 `Chinese_mappings.json` 和 `Chinese_mappings.yaml` 文件生成
   - 验证 `extract_{timestamp}_report.json` 文件生成
   - 验证 `mod_info.json` 文件生成

### 3.3 Extend模式测试
#### 3.3.1 英文映射到中文
1. **执行命令**：`python src/main.py extend "已有英文src文件夹映射流程"`
2. **验证结果**：
   - 检查是否生成 `output/Extend_en2zh/` 目录
   - 验证映射后的源文件夹生成
   - 验证 `English_mappings.json` 和 `English_mappings.yaml` 文件生成
   - 验证 `extend_{timestamp}_report.json` 文件生成
   - 验证 `mod_info.json` 文件生成

#### 3.3.2 中文映射到英文
1. **执行命令**：`python src/main.py extend "已有中文src文件夹映射流程"`
2. **验证结果**：
   - 检查是否生成 `output/Extend_zh2en/` 目录
   - 验证映射后的源文件夹生成
   - 验证 `Chinese_mappings.json` 和 `Chinese_mappings.yaml` 文件生成
   - 验证 `extend_{timestamp}_report.json` 文件生成
   - 验证 `mod_info.json` 文件生成

## 4. 验证要求
1. **文件生成验证**：确认所有预期文件已按要求创建
2. **文件内容验证**：检查文件内容的准确性和完整性
3. **目录结构验证**：确保目录结构符合预期
4. **流程执行验证**：确认流程执行成功，没有错误

## 5. 预期输出
- **Extract模式**：生成映射规则文件、流程报告和mod_info.json
- **Extend模式**：生成映射后的源文件夹、映射规则文件、流程报告和mod_info.json
- **所有文件**：内容准确、格式正确、完整性良好

## 6. 测试结果记录
测试完成后，记录所有测试结果，包括：
- 每个流程的执行状态
- 生成的文件列表
- 文件内容验证结果
- 任何错误或异常情况

## 7. 测试恢复
测试完成后，恢复原始文件和目录结构，确保不影响后续使用