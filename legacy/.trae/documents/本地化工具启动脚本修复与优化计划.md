## 1. 启动脚本修复

### 1.1 修复 `run_tool.bat` 脚本
- **问题**：路径引用错误，无法找到 `main.py` 和虚拟环境
- **修复内容**：
  - 修正 `TOOL_DIR` 定义，确保指向 `Localization_Tool` 目录
  - 修正 `MAIN_PY` 路径，指向正确的 `main.py` 位置
  - 修正虚拟环境路径
  - 添加详细的错误信息和日志
  - 优化脚本逻辑，提高鲁棒性

### 1.2 修复 `run_tool.ps1` 脚本
- **问题**：路径引用错误，无法找到 `main.py` 和虚拟环境
- **修复内容**：
  - 修正 `ToolDir` 定义，确保指向 `Localization_Tool` 目录
  - 修正 `MainPyPath` 路径，指向正确的 `main.py` 位置
  - 修正虚拟环境路径
  - 添加详细的错误信息和日志
  - 优化脚本逻辑，提高鲁棒性

## 2. 脚本本地化重命名

- 将 `run_tool.bat` 重命名为 `启动工具.bat`
- 将 `run_tool.ps1` 重命名为 `启动工具.ps1`

## 3. 文件夹结构优化

### 3.1 路径引用优化
- 确保所有脚本使用相对路径或正确的绝对路径
- 测试脚本在不同目录下的执行情况
- 确保配置文件路径引用正确

### 3.2 分享功能测试
- 测试脚本在压缩包解压后是否能正常运行
- 测试脚本在不同目录结构下的兼容性
- 确保资源文件和配置文件可正确访问

## 4. 创建使用说明书

### 4.1 文档内容
- **软件功能简介**：本地化工具的主要功能和用途
- **系统环境要求**：Python版本、依赖项等
- **安装步骤**：虚拟环境创建、依赖安装等
- **启动方法**：脚本使用说明
- **基本操作指南**：Extract和Extend模式的使用方法
- **常见问题解答**：解决用户可能遇到的问题

### 4.2 文档格式
- 使用Markdown格式编写
- 语言通俗易懂，适合不同技术水平用户
- 包含必要的截图和示例

## 5. 测试验证

- 测试修复后的脚本是否能正常启动
- 测试脚本是否能正确激活虚拟环境
- 测试脚本是否能正确执行本地化工具
- 测试工具的Extract和Extend模式功能
- 测试分享后的功能完整性

## 6. 实施顺序

1. 修复 `run_tool.bat` 脚本
2. 修复 `run_tool.ps1` 脚本
3. 测试修复后的脚本
4. 重命名脚本为中文名称
5. 优化文件夹结构和路径引用
6. 创建使用说明书
7. 进行全面的功能测试
8. 测试分享后的功能完整性