## 修复本地化工具output文件夹结构

### 问题分析
经过对比用户提供的完整文件夹列表和当前代码实现，发现以下差异：

1. **当前代码缺少output子文件夹创建**：
   - 代码中只创建了output根文件夹
   - 缺少Extract_Chinese、Extract_English、Extend_en2zh、Extend_zh2en文件夹

2. **用户要求的文件夹结构**：
   - 所有output子文件夹需要在工具启动时就被创建
   - 这些文件夹是固定的，不是在运行时动态创建

### 修复计划

1. **修改main.py中的文件夹创建列表**：
   - 在localization_folders列表中添加output子文件夹
   - 添加以下文件夹：
     ```python
     os.path.join(localization_file_path, "output", "Extract_Chinese"),
     os.path.join(localization_file_path, "output", "Extract_English"),
     os.path.join(localization_file_path, "output", "Extend_en2zh"),
     os.path.join(localization_file_path, "output", "Extend_zh2en"),
     ```
   - 修改位置：第394行之后，在output文件夹之后添加

2. **验证其他文件夹结构**：
   - 确认source和source_backup文件夹结构正确
   - 确认rule文件夹结构正确

### 修复点

1. **main.py**：
   - 修改第372-395行的localization_folders列表
   - 添加所有必要的output子文件夹

### 验证方法
- 运行工具，检查所有output子文件夹是否被创建
- 验证文件夹结构与用户提供的列表一致
- 确保Extract和Extend模式能够正常使用这些文件夹

### 预期结果
修复后，本地化工具将在启动时创建所有必要的output子文件夹，包括Extract_Chinese、Extract_English、Extend_en2zh和Extend_zh2en，确保工具运行时能够直接使用这些文件夹，符合用户要求的文件夹结构。