# 文件夹重命名、移动和代码适配计划

## 1. 任务概述

将文件夹 `c:\Users\Roki\Documents\GitHub\Tool\legacy\Localization_File` 重命名为 "File"，并将其内容移动到 `c:\Users\Roki\Documents\GitHub\Tool\legacy\Localization_Tool` 下，然后更新所有代码中的路径引用。

## 2. 实施步骤

### 2.1 文件夹操作

1. **重命名文件夹**：将 `Localization_File` 重命名为 `File`
2. **移动文件夹内容**：将 `File` 文件夹中的所有内容移动到 `Localization_Tool` 下

### 2.2 代码适配

1. **配置文件更新**：更新 `directory_config.json` 中的路径配置
2. **核心代码更新**：
   - `config_utils.py`：更新默认配置和路径解析逻辑
   - `init_mode/core.py`：更新文件夹结构定义
   - `main.py`：更新项目结构检查逻辑
3. **其他文件更新**：搜索并更新所有引用 `Localization_File` 的文件
4. **测试验证**：运行项目测试套件，确保所有功能正常

## 3. 具体实施细节

### 3.1 文件夹操作

```bash
# 1. 重命名文件夹
rename "c:\Users\Roki\Documents\GitHub\Tool\legacy\Localization_File" "File"

# 2. 移动文件夹内容
move "c:\Users\Roki\Documents\GitHub\Tool\legacy\File" "c:\Users\Roki\Documents\GitHub\Tool\legacy\Localization_Tool"
```

### 3.2 代码适配

1. **更新 `directory_config.json`**：
   - 将 `mod_root` 从 `${current_dir}/../Localization_File` 改为 `${current_dir}/File`

2. **更新 `config_utils.py`**：
   - 将默认配置中的 `mod_root` 从 `${current_dir}/../Localization_File` 改为 `${current_dir}/File`
   - 更新 `_resolve_directory_config` 方法中的默认配置

3. **更新 `init_mode/core.py`**：
   - 验证 `REQUIRED_FOLDERS` 配置是否正确
   - 更新相关路径引用

4. **更新 `main.py`**：
   - 更新 `check_project_structure` 方法中的路径
   - 更新 `show_welcome_guide` 方法中的路径显示

5. **搜索并更新其他文件**：
   - 使用 `grep` 搜索所有引用 `Localization_File` 的文件
   - 更新所有相关引用

## 4. 测试验证

1. 运行 `python src/main.py -h` 验证工具启动
2. 运行项目测试套件 `pytest test/` 验证所有功能正常
3. 手动测试各模式（Extract、Extend、Decompile）确保路径正确

## 5. 预期结果

- 文件夹结构正确：`c:\Users\Roki\Documents\GitHub\Tool\legacy\Localization_Tool\File`
- 所有代码中的路径引用已更新
- 工具运行正常，无路径相关错误
- 所有测试用例通过