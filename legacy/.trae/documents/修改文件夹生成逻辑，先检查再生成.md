1. 修改 `init_project_structure` 函数，在创建文件夹前先检查文件夹是否存在
2. 只有在文件夹不存在时才调用 `os.makedirs` 创建文件夹
3. 确保只对Terminal#17-26中列出的10个必要文件夹执行此检查和生成逻辑
4. 保持其他代码逻辑不变

具体修改点：

* 在 `init_project_structure` 函数中，将直接调用 `os.makedirs` 的逻辑改为先检查 `os.path.exists`

* 只有当文件夹不存在时，才创建文件夹并记录日志

* 确保只对 REQUIRED\_FOLDERS 列表中的文件夹执行此操作

